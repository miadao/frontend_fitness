const EditRoutines = ({}) => {

    return(
        <div>
            <h1 className="Edit"> </h1>
            <button type="submit"> Edit</button>
        </div>
    )
}

export default EditRoutines;
